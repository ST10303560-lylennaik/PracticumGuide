<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:ignore="ExtraText">

        <!-- Playlist Title -->
        <TextView
            android:id="@+id/txtPlaylistDetails"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/playlist_details"
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_marginBottom="12dp"/>

        <!-- Display Songs in Playlist -->
        <TextView
            android:id="@+id/txtSongs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
        android:textSize="16sp"
        android:layout_marginBottom="20dp"/>

        <!-- Calculate Average Rating Button -->
        <Button
            android:id="@+id/btnCalculateAverage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/calculate_average_rating"
            android:layout_marginBottom="12dp"
            tools:ignore="VisualLintButtonSize" />

        <!-- Display Average Rating -->
        <TextView
            android:id="@+id/txtAverageRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="16sp"
            android:textStyle="italic"
            android:layout_marginBottom="20dp"/>

        <!-- Return to Main Screen Button -->
        <Button
            android:id="@+id/btnBackToMain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/back_to_main_menu"
            tools:ignore="VisualLintButtonSize" />
    </LinearLayout>
</ScrollView>
